<%= form_with(model: cart_saved) do |form| %>
  <% if cart_saved.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cart_saved.errors.count, "error") %> prohibited this cart_saved from being saved:</h2>

      <ul>
        <% cart_saved.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity %>
  </div>

  <div>
    <%= form.label :abandoned, style: "display: block" %>
    <%= form.check_box :abandoned %>
  </div>

  <div>
    <%= form.label :item_id, style: "display: block" %>
    <%= form.text_field :item_id %>
  </div>

  <div>
    <%= form.label :profile_id, style: "display: block" %>
    <%= form.text_field :profile_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
