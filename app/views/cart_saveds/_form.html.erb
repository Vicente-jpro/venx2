<%= form_with(model: cart_saved) do |form| %>
  <% if cart_saved.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cart_saved.errors.count, "error") %> prohibited this cart_saved from being saved:</h2>

      <ul>
        <% cart_saved.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

   <div class="d-grid gap-2 my-1">
    <%= form.submit "Salvar para vender depois", class: "btn btn-outline-success"%>
  </div>
<% end %>
