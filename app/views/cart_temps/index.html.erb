  <section>
    <h5 class="fs-3 text-center">sales panel <%= current_user.email%></h5>
    <div class="row">
      <div class="col-sm-4 fs-4 text-success">
        <strong>Total:</strong> <span class="total"><%=  @total_cost %></span> 
        <br/>
        <br/>
      </div>
      <div class="col">

      </div>
     <div class="col">
        <%= render "invoice_temps/form", invoice_temp: InvoiceTemp.new %>
      </div>
    </div>
    <br/><br/>
      <%= link_to "Nova venda", new_sale_cart_temps_path, method: :delete, class: "btn btn-warning", 
          form:{ data: { turbo_confirm: "Deseja efetuar uma nova venda?"} }%> 
  
    <table class="table table-hover shadow-sm">
        <thead>
          <tr>
            <th scope="col">Código</th>
            <th scope="col">Nome/Descrição</th>
            <th scope="col">Q.Stock</th>
            <th scope="col">P.Unitário</th>
            <th scope="col">Sub.total</th>
            <th scope="col">V.Pagar</th>
            <th scope="col">Lucro</th>
            <th scope="col">D.Expiracao</th>
            <th scope="col">Abandoned</th>
            <th scope="col">Acrescentar</th>
            <th scope="col">Diminuir</th>
          </tr>
        </thead>
        <tbody>
          <div id="cart_temps">
            <% @cart_temps.each do |cart_temp| %>
              <%= render cart_temp %>
            <% end %>
          </div>
        </tbody>
      </table>

  </section>

  <!-- Fim Itens index-->







